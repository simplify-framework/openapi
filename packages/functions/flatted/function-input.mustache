{
    "Profile": "{{deploymentProfileOrigin}}",
    "Region": "{{deploymentRegionOrigin}}",
    "Bucket": {
        "Name": "{{projectNamePosix}}-{{projectId}}",
        "Key": "functions/{{serviceNamePosix}}/${DATE_TODAY}/dist"
    },
    "OutputFile": "function-meta.json",
    "LayerConfig": {
        "CompatibleRuntimes": [
            "{{serviceRuntimeOrigin}}"
        ],
        "LayerName": "{{serviceNamePosix}}"
    },
    "Function": {
        "FunctionName": "{{serviceName}}",
        "Handler": "index.handler",
        "MemorySize": 128,
        "Publish": true,
        "Role": "${Function{{serviceNamePascal}}RoleArn}",
        "Runtime": "{{serviceRuntimeOrigin}}",
        "Tags": {
            "GroupName": "{{serviceTag}}"
        },
        "Timeout": 15,
        "TracingConfig": {
            "Mode": "PassThrough"
        },
        "Environment": {
            "Variables": {
                "DEPLOYMENT_STAGE": "${DEPLOYMENT_STAGE}",
                "ENFORCEMENT_PACKAGE": "${ENFORCEMENT_PACKAGE}"
            }
        }
    }
}